interface:
  display_name: "sdlc orchestrator"
  short_description: "사용자가 단일 요청으로 서비스 전체 제작을 원할 때 기획(mobile-web-planner), 구현(nextjs-implementer), 보안(finguard)을 순차적으로 위임하고 통제하는 총괄 PM 메타 스킬"
  default_prompt: "$sdlc-orchestrator 로 작업해줘."
